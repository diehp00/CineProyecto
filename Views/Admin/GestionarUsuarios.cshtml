@model List<Usuario>

@{
    ViewData["Title"] = "Gestionar Usuarios";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>@ViewData["Title"]</title>
    <style>
        .table-actions a {
            margin-right: 5px;
        }
    </style>
</head>
<body class="bg-light">
    <div class="container my-5">
        <!-- Encabezado -->
        <h2 class="text-center text-primary mb-4">Gestionar Usuarios</h2>
        
        <!-- Tabla -->
        <div class="table-responsive">
            <table class="table table-hover table-bordered align-middle">
                <thead class="table-danger">
                    <tr>
                        <th scope="col">Nombre de Usuario</th>
                        <th scope="col">Rol</th>
                        <th scope="col">Correo Electronico</th>
                        <th scope="col" class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var usuario in Model)
                    {
                        <tr>
                            <td>@usuario.NombreUsuario</td>
                            <td>@usuario.Rol</td>
                            <td>@usuario.CorreoElectronico</td>
                            <td class="text-center">
                                <div class="table-actions">
                                    <a class="btn btn-warning btn-sm" href="/Usuarios/Edit/@usuario.Id">Editar</a>
                                    <form asp-action="Delete" asp-controller="Usuarios" method="post" style="display:inline;">
                                        <input type="hidden" name="id" value="@usuario.Id" />
                                        <a class="btn btn-danger btn-sm" href="/Usuarios/Delete/@usuario.Id">Eliminar</a>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Botón para agregar usuario -->
        <div class="text-center">
            <a class="btn btn-danger mt-3" href="/Admin/AdministradorDashboard">Cancelar</a>
            <a class="btn btn-success mt-3" href="/Registro">Agregar Usuario</a>
        </div>
    </div>

    <!-- Script de Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
